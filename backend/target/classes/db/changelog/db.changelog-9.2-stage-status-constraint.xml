<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="2026-04-30-02-stage-status-constraint" author="copilot">
        <preConditions onFail="MARK_RAN" onError="HALT">
            <tableExists tableName="SENDER_STAGE"/>
        </preConditions>

        <!-- Drop any existing CHECK constraint on SENDER_STAGE.status (Oracle PL/SQL).
             splitStatements="false" is required so Liquibase sends the entire block
             as a single statement instead of splitting on semicolons. -->
        <sql splitStatements="false">
            DECLARE
            v_name VARCHAR2(200);
            BEGIN
            FOR r IN (
            SELECT constraint_name
            FROM user_constraints
            WHERE table_name = 'SENDER_STAGE'
            AND constraint_type = 'C'
            ) LOOP
            BEGIN
            EXECUTE IMMEDIATE 'ALTER TABLE SENDER_STAGE DROP CONSTRAINT "' || r.constraint_name || '"';
            EXCEPTION WHEN OTHERS THEN
            NULL;
            END;
            END LOOP;
            END;
        </sql>

        <!-- Migrate any existing PROCESSING rows to ENRICHMENT before adding the constraint -->
        <sql splitStatements="false">
            UPDATE SENDER_STAGE SET status = 'ENRICHMENT' WHERE status = 'PROCESSING'
        </sql>

        <!-- Add canonical CHECK constraint with the full status lifecycle -->
        <sql splitStatements="false">
            ALTER TABLE SENDER_STAGE ADD CONSTRAINT chk_sender_stage_status
            CHECK (status IN ('NEW','ENQUEUED','ENRICHMENT','EXENSIO_LOADING','DONE','FAILED','CANCELLED'))
        </sql>
    </changeSet>

</databaseChangeLog>
